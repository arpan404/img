{
  "type": "object",
  "styles": {
    "patternProperties": {
      "^[a-zA-Z0-9]+$": {
        "type": "object",
        "properties": {
          "prompt": "string",
          "language": "string",
          "video": "string"
        },
        "required": [
          "prompt",
          "language",
          "video"
        ]
      }
    }
  },
  "required": [
    "styles"
  ]
}